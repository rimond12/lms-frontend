// This is a default fallback for the @auth parallel route
// You can customize this based on your authentication needs

export default function DefaultAuth() {
  // Return null if you don't want to render anything by default
  // Or return your authentication component/provider
  return null;
}
